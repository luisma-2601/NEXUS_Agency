# URL del webhook de n8n que recibe los mensajes del chatbot.
# El flujo de n8n debe recibir { message, sessionId } y responder { reply }.
# Si se deja sin definir, el chatbot usa únicamente el respaldo local de FAQs.
VITE_N8N_WEBHOOK_URL=
